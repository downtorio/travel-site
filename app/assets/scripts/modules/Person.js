/* jshint esversion:6 */

/*class Person {
	constructor(fullName, faveColor) {
		this.name = fullName;
		this.favoriteColor = faveColor;
	}

	greet() {
		console.log(`Hey gorl, my name is ${this.name} and my favorite color is ${this.favoriteColor}.`);
	}
}

//'exports' is what will be returned by a 'require' call [which is called from app.js]
//so we target exports's parent and tell it that exports should be equal to our Person
//module.exports = Person;		//again, let's do this the ES6 way:
export default Person;*/

